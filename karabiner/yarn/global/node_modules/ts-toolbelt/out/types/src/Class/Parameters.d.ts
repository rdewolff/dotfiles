/** Get the parameters of a class constructor
 * @param C **typeof** **`class`**
 * @returns **`any[]`**
 * @example
 * ```ts
 * ```
 */
export declare type Parameters<C extends new (...args: any[]) => any> = C extends new (...args: infer P) => any ? P : never;
