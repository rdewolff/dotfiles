import { AssignUp as OAssignUp } from '../Object/AssignUp';
import { Omit } from './Omit';
import { Tuple } from './Tuple';
import { ObjectOf } from './ObjectOf';
import { Cast } from '../Any/Cast';
/** Assign a list of **tuple** into **`T`** with **`MergeUp`** (last-in combines or overrides)
 * @param T to assign to
 * @param Ts to assign
 * @returns **`object`**
 * @example
 * ```ts
 * ```
 */
export declare type AssignUp<T extends Tuple, Ts extends Tuple<Tuple>> = Omit<OAssignUp<T, {
    [K in keyof Ts]: ObjectOf<Cast<Ts[K], Tuple>>;
}> & [], keyof any[]>;
