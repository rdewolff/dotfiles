import { IterationOf } from '../../Iteration/IterationOf';
import { Iteration } from '../../Iteration/Iteration';
import { Pos } from '../../Iteration/Pos';
import { Next } from '../../Iteration/Next';
import { Path as PPath } from './_Internal';
import { Index } from '../../Any/Index';
import { Pick as OPick } from '../Pick';
import { LastIndex } from '../../Tuple/LastIndex';
import { Tuple } from '../../Tuple/Tuple';
import { Key } from '../../Iteration/Key';
declare type _Pick<O extends object, Path extends Tuple<Index>, I extends Iteration = IterationOf<'0'>> = OPick<O, Path[Pos<I>]> extends infer Picked ? {
    [K in keyof Picked]: Picked[K] extends infer Prop ? Prop extends object ? Key<I> extends LastIndex<Path, 's'> ? Prop : _Pick<Prop, Path, Next<I>> : Prop : never;
} & {} : never;
/** Extract out of **`O`** the fields at **`Path`**
 * (⚠️ this type is expensive)
 * @param O to extract from
 * @param Path to be followed
 * @returns **`object`**
 * @example
 * ```ts
 * ```
 */
export declare type Pick<O extends object, Path extends PPath> = _Pick<O, Path>;
export {};
