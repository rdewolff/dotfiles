import { Merge as OMerge } from '../Merge';
import { IterationOf } from '../../Iteration/IterationOf';
import { Iteration } from '../../Iteration/Iteration';
import { Pos } from '../../Iteration/Pos';
import { Next } from '../../Iteration/Next';
import { Path as PPath } from './_Internal';
import { Index } from '../../Any/Index';
import { LastIndex } from '../../Tuple/LastIndex';
import { Depth } from '../_Internal';
import { Tuple } from '../../Tuple/Tuple';
import { Key } from '../../Iteration/Key';
declare type _Merge<O extends object, Path extends Tuple<Index>, O1 extends object, depth extends Depth, I extends Iteration = IterationOf<'0'>> = {
    [K in keyof O]: O[K] extends infer Prop ? K extends Path[Pos<I>] ? Prop extends object ? Key<I> extends LastIndex<Path, 's'> ? OMerge<Prop, O1, depth> : _Merge<Prop, Path, O1, depth, Next<I>> : Prop : Prop : never;
} & {};
/** Complete the fields of **`O`** at **`Path`** with the ones of **`O1`**
 * (⚠️ this type is expensive)
 * @param O to complete
 * @param Path to be followed
 * @param O1 to copy from
 * @param depth to do it deeply (?=`'flat'`)
 * @returns **`object`**
 * @example
 * ```ts
 * ```
 */
export declare type Merge<O extends object, Path extends PPath, O1 extends object, depth extends Depth = 'flat'> = _Merge<O, Path, O1, depth>;
export {};
