import { BaseCommandManager } from './BaseCommandManager';
export declare class Git extends BaseCommandManager {
    static run(cwd: string, ...args: string[]): Promise<void>;
    static clone(remote: string, projectName?: string, cwd?: string): Promise<void>;
}
