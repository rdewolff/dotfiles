/// <reference types="node" />
import * as fs from 'fs';
import { Stream } from "stream";
export declare type ConfigType = Partial<{
    sourceDirectory: string;
    targetDirectory: string;
    sourceFile: string;
    targetFile: string;
}>;
export declare type ModifyType = Partial<{
    append: string;
    prepend: string;
    replace: string;
    custom: Function;
}>;
export declare type StreamType = fs.ReadStream | fs.WriteStream;
export declare type TapeRollerConfigType = {
    templateDirectory: string;
};
export declare type SubjectType = {
    file: string;
    stream: Stream;
};
