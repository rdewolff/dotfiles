import { BaseCommandManager } from './BaseCommandManager';
export declare class Package extends BaseCommandManager {
    static run(yarn: string, npm: string, ...both: string[]): Promise<void>;
    static add(dependency: string, dev?: boolean): Promise<void>;
    static install(cwd?: string): Promise<void>;
}
