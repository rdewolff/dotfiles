/// <reference types="node" />
import { Writable } from 'stream';
import { ModifyType, SubjectType } from './types';
import { IOptions } from 'glob';
export declare const TMP_POSTFIX = "___TMP";
export declare class FileTask {
    private subjects;
    private readonly concatFileName;
    private options;
    constructor(files: string | string[], concatFileName?: boolean, options?: IOptions);
    private addSubject;
    write(destinationName?: string): Promise<void[]>;
    update(): Promise<void[]>;
    writeFile(subject: SubjectType, destinationName: string): Promise<void>;
    replace(parameters: {
        [key: string]: any;
    }): this;
    modify(pattern: RegExp, { append, prepend, custom, replace }: ModifyType): this;
    pipe(through: () => Writable): this;
}
